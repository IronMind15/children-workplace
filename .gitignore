# dependencies
/node_modules

# next.js
/.next/
/.next_bak/
/.next_bak_*/
/out/

# production build
/build

# misc
.DS_Store
*.pem

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env*

# typescript / next generated
*.tsbuildinfo
next-env.d.ts

# local runtime database (regenerated by seedIfEmpty on first run)
# 整目录忽略：内容随代码 seed 生成（方案 B 后仅玩家库），任何 .db/.db-shm/.db-wal/.bak 伴随文件都不入库
/data/

# workbuddy local agent memory (project-private, not for the repo)
/.workbuddy

# MicroEngine 公网部署（含 session_token 等短期凭据，严禁入库）
/.microengine-deploy/

# large demo recordings (use Git LFS if you want them tracked)
docs/*.mp4

# 原始大体积参考图（游戏内已用压缩 WebP，原始图本地留存不入库）
docs/数学世界地图.png

# UI 设计稿大文件（zip + 解包目录，不入库）
docs/112初版*
docs/113背景*
docs/114图标*
docs/115保卫小兵*
docs/116战斗背景*
docs/117宠物和我_抠图版*
docs/118群岛*

# bug 截图（用户提供的工作截图，不入库）
docs/bug修复*

# vercel
.vercel

# 原始 PNG 素材备份（游戏内已用压缩 WebP，原始图本地留存不入库）
/assets_backup

# 奥数/袋鼠数学真题素材（版权材料 + 大体积 zip/pdf，不入库）
docs/奥数题目*

# electron-builder 打包输出（便携 exe 等大产物，不入库）
/dist/

# 外部打包/测试工具产物（源码归档 zip、etest 测试目录，不入库）
/.etest2/
知识岛-源代码-*.zip
